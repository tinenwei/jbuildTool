source-path: # absolute path or relative to current direcotry.

source-search-path: # absolute path or relative to current direcotry. Use Source-Search-Key (default: java)

class-path: # absolute path or relative to current direcotry.

jar-search-path: # absolute path or relative to current direcotry. (*.jar will be searched)



library-dir: # default: ./lib
classes-dir: # output classes for build, default: ./bin
source-dir: # soures path for build (support multiple paths), default: ./src/main/java

Main-Class:

cfg:

